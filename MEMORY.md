# Memory

## Immutable Rule: `SERVE X`

Interpret `SERVE X` exactly as:
1. Find `X` as a Lua file under `~/projects`.
2. Use `mittens` CLI to serve it.
3. Do not use repo `examples/` for `SERVE X` unless explicitly requested.
